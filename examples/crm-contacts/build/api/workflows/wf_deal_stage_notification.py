"""Workflow: deal_stage_notification — generated by doql."""
from workflows.engine import WorkflowRun, Step, engine

# Trigger: deal.updated



def create(context: dict) -> WorkflowRun:
    return WorkflowRun(
        workflow_name="deal_stage_notification",
        context=context,
        steps=[

        ],
    )


# Register with engine
engine.register("deal_stage_notification", create)
