"""Workflow: daily_overdue_check — generated by doql."""
from workflows.engine import WorkflowRun, Step, engine

# Trigger: schedule "0 8 * * *"



def create(context: dict) -> WorkflowRun:
    return WorkflowRun(
        workflow_name="daily_overdue_check",
        context=context,
        steps=[

        ],
    )


# Register with engine
engine.register("daily_overdue_check", create)
