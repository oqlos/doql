# CRM Contacts — API

Generated by `doql build`.

## Quick start

```bash
cd build/api
pip install -r requirements.txt
uvicorn main:app --reload
```

## Entities

- **Contact** (12 fields)
- **Company** (8 fields)
- **Deal** (13 fields)
- **Activity** (10 fields)

## Endpoints

- `GET /api/v1/contacts` · `POST` · `GET /:id` · `PATCH /:id` · `DELETE /:id`
- `GET /api/v1/companys` · `POST` · `GET /:id` · `PATCH /:id` · `DELETE /:id`
- `GET /api/v1/deals` · `POST` · `GET /:id` · `PATCH /:id` · `DELETE /:id`
- `GET /api/v1/activitys` · `POST` · `GET /:id` · `PATCH /:id` · `DELETE /:id`
