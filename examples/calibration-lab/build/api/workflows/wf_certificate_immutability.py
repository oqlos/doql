"""Workflow: certificate_immutability — generated by doql."""
from workflows.engine import WorkflowRun, Step, engine

# Trigger: event Calibration.signed



def create(context: dict) -> WorkflowRun:
    return WorkflowRun(
        workflow_name="certificate_immutability",
        context=context,
        steps=[

        ],
    )


# Register with engine
engine.register("certificate_immutability", create)
