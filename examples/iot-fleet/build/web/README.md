# IoT Fleet Manager — Web

Generated by `doql build`. React + Vite + TailwindCSS.

## Quick start

```bash
cd build/web
npm install
npm run dev
```

## Pages

- **Dashboard** — entity counts overview
- **Nodes** — CRUD list/create/delete
- **Telemetrys** — CRUD list/create/delete
- **Deployments** — CRUD list/create/delete
- **FirmwareBuilds** — CRUD list/create/delete
- **OTAUpdates** — CRUD list/create/delete
