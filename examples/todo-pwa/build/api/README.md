# Todo PWA — API

Generated by `doql build`.

## Quick start

```bash
cd build/api
pip install -r requirements.txt
uvicorn main:app --reload
```

## Entities

- **Todo** (6 fields)

## Endpoints

- `GET /api/v1/todos` · `POST` · `GET /:id` · `PATCH /:id` · `DELETE /:id`
