# Generated by doql build for Inspection Kiosk Station
services:

  api:
    build:
      context: ../api
      dockerfile: ../infra/Dockerfile
    container_name: inspection-kiosk-station-api
    restart: unless-stopped
    env_file: .env.docker
    ports:
      - '8000:8000'
    volumes:
      - data:/app/data

volumes:
  data:
