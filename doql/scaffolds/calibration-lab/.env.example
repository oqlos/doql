APP_NAME="Calibration Lab"
APP_URL=http://localhost:8080
APP_ENV=dev

DATABASE_URL=postgresql://user:pass@localhost:5432/callab

JWT_SECRET=change_me_generate_with_openssl_rand_hex_32
JWT_EXPIRE_MINUTES=480

# Email notifications for calibration reminders
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=noreply@lab.example.com
SMTP_PASS=change_me
SMTP_FROM=noreply@lab.example.com

# Recommended plugins:
#   pip install doql-plugin-iso17025  # uncertainty + traceability
#   pip install doql-plugin-gxp       # audit log + e-signatures
