"""Workflow: comment_moderation — generated by doql."""
from workflows.engine import WorkflowRun, Step, engine

# Trigger: comment.created



def create(context: dict) -> WorkflowRun:
    return WorkflowRun(
        workflow_name="comment_moderation",
        context=context,
        steps=[

        ],
    )


# Register with engine
engine.register("comment_moderation", create)
