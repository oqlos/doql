"""CRUD routes — generated by doql."""
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from database import get_db
import models
import schemas

router = APIRouter(prefix='/api/v1')

# ── User ──

@router.get("/users", response_model=list[schemas.UserResponse])
def list_users(skip: int = 0, limit: int = 100, db: Session = Depends(get_db)):
    return db.query(models.User).offset(skip).limit(limit).all()

@router.get("/users/{item_id}", response_model=schemas.UserResponse)
def get_user(item_id: str, db: Session = Depends(get_db)):
    obj = db.query(models.User).filter(models.User.id == item_id).first()
    if not obj:
        raise HTTPException(404, "User not found")
    return obj

@router.post("/users", response_model=schemas.UserResponse, status_code=201)
def create_user(data: schemas.UserCreate, db: Session = Depends(get_db)):
    import uuid as _uuid
    payload = data.model_dump()
    payload.setdefault('id', str(_uuid.uuid4()))
    obj = models.User(**payload)
    db.add(obj)
    db.commit()
    db.refresh(obj)
    return obj

@router.patch("/users/{item_id}", response_model=schemas.UserResponse)
def update_user(item_id: str, data: schemas.UserUpdate, db: Session = Depends(get_db)):
    obj = db.query(models.User).filter(models.User.id == item_id).first()
    if not obj:
        raise HTTPException(404, "User not found")
    for key, val in data.model_dump(exclude_unset=True).items():
        setattr(obj, key, val)
    db.commit()
    db.refresh(obj)
    return obj

@router.delete("/users/{item_id}", status_code=204)
def delete_user(item_id: str, db: Session = Depends(get_db)):
    obj = db.query(models.User).filter(models.User.id == item_id).first()
    if not obj:
        raise HTTPException(404, "User not found")
    db.delete(obj)
    db.commit()

# ── Station ──

@router.get("/stations", response_model=list[schemas.StationResponse])
def list_stations(skip: int = 0, limit: int = 100, db: Session = Depends(get_db)):
    return db.query(models.Station).offset(skip).limit(limit).all()

@router.get("/stations/{item_id}", response_model=schemas.StationResponse)
def get_station(item_id: str, db: Session = Depends(get_db)):
    obj = db.query(models.Station).filter(models.Station.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Station not found")
    return obj

@router.post("/stations", response_model=schemas.StationResponse, status_code=201)
def create_station(data: schemas.StationCreate, db: Session = Depends(get_db)):
    import uuid as _uuid
    payload = data.model_dump()
    payload.setdefault('id', str(_uuid.uuid4()))
    obj = models.Station(**payload)
    db.add(obj)
    db.commit()
    db.refresh(obj)
    return obj

@router.patch("/stations/{item_id}", response_model=schemas.StationResponse)
def update_station(item_id: str, data: schemas.StationUpdate, db: Session = Depends(get_db)):
    obj = db.query(models.Station).filter(models.Station.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Station not found")
    for key, val in data.model_dump(exclude_unset=True).items():
        setattr(obj, key, val)
    db.commit()
    db.refresh(obj)
    return obj

@router.delete("/stations/{item_id}", status_code=204)
def delete_station(item_id: str, db: Session = Depends(get_db)):
    obj = db.query(models.Station).filter(models.Station.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Station not found")
    db.delete(obj)
    db.commit()

# ── Operator ──

@router.get("/operators", response_model=list[schemas.OperatorResponse])
def list_operators(skip: int = 0, limit: int = 100, db: Session = Depends(get_db)):
    return db.query(models.Operator).offset(skip).limit(limit).all()

@router.get("/operators/{item_id}", response_model=schemas.OperatorResponse)
def get_operator(item_id: str, db: Session = Depends(get_db)):
    obj = db.query(models.Operator).filter(models.Operator.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Operator not found")
    return obj

@router.post("/operators", response_model=schemas.OperatorResponse, status_code=201)
def create_operator(data: schemas.OperatorCreate, db: Session = Depends(get_db)):
    import uuid as _uuid
    payload = data.model_dump()
    payload.setdefault('id', str(_uuid.uuid4()))
    obj = models.Operator(**payload)
    db.add(obj)
    db.commit()
    db.refresh(obj)
    return obj

@router.patch("/operators/{item_id}", response_model=schemas.OperatorResponse)
def update_operator(item_id: str, data: schemas.OperatorUpdate, db: Session = Depends(get_db)):
    obj = db.query(models.Operator).filter(models.Operator.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Operator not found")
    for key, val in data.model_dump(exclude_unset=True).items():
        setattr(obj, key, val)
    db.commit()
    db.refresh(obj)
    return obj

@router.delete("/operators/{item_id}", status_code=204)
def delete_operator(item_id: str, db: Session = Depends(get_db)):
    obj = db.query(models.Operator).filter(models.Operator.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Operator not found")
    db.delete(obj)
    db.commit()

# ── Qualification ──

@router.get("/qualifications", response_model=list[schemas.QualificationResponse])
def list_qualifications(skip: int = 0, limit: int = 100, db: Session = Depends(get_db)):
    return db.query(models.Qualification).offset(skip).limit(limit).all()

@router.get("/qualifications/{item_id}", response_model=schemas.QualificationResponse)
def get_qualification(item_id: str, db: Session = Depends(get_db)):
    obj = db.query(models.Qualification).filter(models.Qualification.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Qualification not found")
    return obj

@router.post("/qualifications", response_model=schemas.QualificationResponse, status_code=201)
def create_qualification(data: schemas.QualificationCreate, db: Session = Depends(get_db)):
    import uuid as _uuid
    payload = data.model_dump()
    payload.setdefault('id', str(_uuid.uuid4()))
    obj = models.Qualification(**payload)
    db.add(obj)
    db.commit()
    db.refresh(obj)
    return obj

@router.patch("/qualifications/{item_id}", response_model=schemas.QualificationResponse)
def update_qualification(item_id: str, data: schemas.QualificationUpdate, db: Session = Depends(get_db)):
    obj = db.query(models.Qualification).filter(models.Qualification.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Qualification not found")
    for key, val in data.model_dump(exclude_unset=True).items():
        setattr(obj, key, val)
    db.commit()
    db.refresh(obj)
    return obj

@router.delete("/qualifications/{item_id}", status_code=204)
def delete_qualification(item_id: str, db: Session = Depends(get_db)):
    obj = db.query(models.Qualification).filter(models.Qualification.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Qualification not found")
    db.delete(obj)
    db.commit()

# ── Device ──

@router.get("/devices", response_model=list[schemas.DeviceResponse])
def list_devices(skip: int = 0, limit: int = 100, db: Session = Depends(get_db)):
    return db.query(models.Device).offset(skip).limit(limit).all()

@router.get("/devices/{item_id}", response_model=schemas.DeviceResponse)
def get_device(item_id: str, db: Session = Depends(get_db)):
    obj = db.query(models.Device).filter(models.Device.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Device not found")
    return obj

@router.post("/devices", response_model=schemas.DeviceResponse, status_code=201)
def create_device(data: schemas.DeviceCreate, db: Session = Depends(get_db)):
    import uuid as _uuid
    payload = data.model_dump()
    payload.setdefault('id', str(_uuid.uuid4()))
    obj = models.Device(**payload)
    db.add(obj)
    db.commit()
    db.refresh(obj)
    return obj

@router.patch("/devices/{item_id}", response_model=schemas.DeviceResponse)
def update_device(item_id: str, data: schemas.DeviceUpdate, db: Session = Depends(get_db)):
    obj = db.query(models.Device).filter(models.Device.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Device not found")
    for key, val in data.model_dump(exclude_unset=True).items():
        setattr(obj, key, val)
    db.commit()
    db.refresh(obj)
    return obj

@router.delete("/devices/{item_id}", status_code=204)
def delete_device(item_id: str, db: Session = Depends(get_db)):
    obj = db.query(models.Device).filter(models.Device.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Device not found")
    db.delete(obj)
    db.commit()

# ── Inspection ──

@router.get("/inspections", response_model=list[schemas.InspectionResponse])
def list_inspections(skip: int = 0, limit: int = 100, db: Session = Depends(get_db)):
    return db.query(models.Inspection).offset(skip).limit(limit).all()

@router.get("/inspections/{item_id}", response_model=schemas.InspectionResponse)
def get_inspection(item_id: str, db: Session = Depends(get_db)):
    obj = db.query(models.Inspection).filter(models.Inspection.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Inspection not found")
    return obj

@router.post("/inspections", response_model=schemas.InspectionResponse, status_code=201)
def create_inspection(data: schemas.InspectionCreate, db: Session = Depends(get_db)):
    import uuid as _uuid
    payload = data.model_dump()
    payload.setdefault('id', str(_uuid.uuid4()))
    obj = models.Inspection(**payload)
    db.add(obj)
    db.commit()
    db.refresh(obj)
    return obj

@router.patch("/inspections/{item_id}", response_model=schemas.InspectionResponse)
def update_inspection(item_id: str, data: schemas.InspectionUpdate, db: Session = Depends(get_db)):
    obj = db.query(models.Inspection).filter(models.Inspection.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Inspection not found")
    for key, val in data.model_dump(exclude_unset=True).items():
        setattr(obj, key, val)
    db.commit()
    db.refresh(obj)
    return obj

@router.delete("/inspections/{item_id}", status_code=204)
def delete_inspection(item_id: str, db: Session = Depends(get_db)):
    obj = db.query(models.Inspection).filter(models.Inspection.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Inspection not found")
    db.delete(obj)
    db.commit()

# ── CylinderFill ──

@router.get("/cylinder_fills", response_model=list[schemas.CylinderFillResponse])
def list_cylinder_fills(skip: int = 0, limit: int = 100, db: Session = Depends(get_db)):
    return db.query(models.CylinderFill).offset(skip).limit(limit).all()

@router.get("/cylinder_fills/{item_id}", response_model=schemas.CylinderFillResponse)
def get_cylinder_fill(item_id: str, db: Session = Depends(get_db)):
    obj = db.query(models.CylinderFill).filter(models.CylinderFill.id == item_id).first()
    if not obj:
        raise HTTPException(404, "CylinderFill not found")
    return obj

@router.post("/cylinder_fills", response_model=schemas.CylinderFillResponse, status_code=201)
def create_cylinder_fill(data: schemas.CylinderFillCreate, db: Session = Depends(get_db)):
    import uuid as _uuid
    payload = data.model_dump()
    payload.setdefault('id', str(_uuid.uuid4()))
    obj = models.CylinderFill(**payload)
    db.add(obj)
    db.commit()
    db.refresh(obj)
    return obj

@router.patch("/cylinder_fills/{item_id}", response_model=schemas.CylinderFillResponse)
def update_cylinder_fill(item_id: str, data: schemas.CylinderFillUpdate, db: Session = Depends(get_db)):
    obj = db.query(models.CylinderFill).filter(models.CylinderFill.id == item_id).first()
    if not obj:
        raise HTTPException(404, "CylinderFill not found")
    for key, val in data.model_dump(exclude_unset=True).items():
        setattr(obj, key, val)
    db.commit()
    db.refresh(obj)
    return obj

@router.delete("/cylinder_fills/{item_id}", status_code=204)
def delete_cylinder_fill(item_id: str, db: Session = Depends(get_db)):
    obj = db.query(models.CylinderFill).filter(models.CylinderFill.id == item_id).first()
    if not obj:
        raise HTTPException(404, "CylinderFill not found")
    db.delete(obj)
    db.commit()

# ── Exercise ──

@router.get("/exercises", response_model=list[schemas.ExerciseResponse])
def list_exercises(skip: int = 0, limit: int = 100, db: Session = Depends(get_db)):
    return db.query(models.Exercise).offset(skip).limit(limit).all()

@router.get("/exercises/{item_id}", response_model=schemas.ExerciseResponse)
def get_exercise(item_id: str, db: Session = Depends(get_db)):
    obj = db.query(models.Exercise).filter(models.Exercise.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Exercise not found")
    return obj

@router.post("/exercises", response_model=schemas.ExerciseResponse, status_code=201)
def create_exercise(data: schemas.ExerciseCreate, db: Session = Depends(get_db)):
    import uuid as _uuid
    payload = data.model_dump()
    payload.setdefault('id', str(_uuid.uuid4()))
    obj = models.Exercise(**payload)
    db.add(obj)
    db.commit()
    db.refresh(obj)
    return obj

@router.patch("/exercises/{item_id}", response_model=schemas.ExerciseResponse)
def update_exercise(item_id: str, data: schemas.ExerciseUpdate, db: Session = Depends(get_db)):
    obj = db.query(models.Exercise).filter(models.Exercise.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Exercise not found")
    for key, val in data.model_dump(exclude_unset=True).items():
        setattr(obj, key, val)
    db.commit()
    db.refresh(obj)
    return obj

@router.delete("/exercises/{item_id}", status_code=204)
def delete_exercise(item_id: str, db: Session = Depends(get_db)):
    obj = db.query(models.Exercise).filter(models.Exercise.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Exercise not found")
    db.delete(obj)
    db.commit()

# ── Deployment ──

@router.get("/deployments", response_model=list[schemas.DeploymentResponse])
def list_deployments(skip: int = 0, limit: int = 100, db: Session = Depends(get_db)):
    return db.query(models.Deployment).offset(skip).limit(limit).all()

@router.get("/deployments/{item_id}", response_model=schemas.DeploymentResponse)
def get_deployment(item_id: str, db: Session = Depends(get_db)):
    obj = db.query(models.Deployment).filter(models.Deployment.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Deployment not found")
    return obj

@router.post("/deployments", response_model=schemas.DeploymentResponse, status_code=201)
def create_deployment(data: schemas.DeploymentCreate, db: Session = Depends(get_db)):
    import uuid as _uuid
    payload = data.model_dump()
    payload.setdefault('id', str(_uuid.uuid4()))
    obj = models.Deployment(**payload)
    db.add(obj)
    db.commit()
    db.refresh(obj)
    return obj

@router.patch("/deployments/{item_id}", response_model=schemas.DeploymentResponse)
def update_deployment(item_id: str, data: schemas.DeploymentUpdate, db: Session = Depends(get_db)):
    obj = db.query(models.Deployment).filter(models.Deployment.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Deployment not found")
    for key, val in data.model_dump(exclude_unset=True).items():
        setattr(obj, key, val)
    db.commit()
    db.refresh(obj)
    return obj

@router.delete("/deployments/{item_id}", status_code=204)
def delete_deployment(item_id: str, db: Session = Depends(get_db)):
    obj = db.query(models.Deployment).filter(models.Deployment.id == item_id).first()
    if not obj:
        raise HTTPException(404, "Deployment not found")
    db.delete(obj)
    db.commit()
