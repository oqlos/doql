"""Workflow: order_confirmation — generated by doql."""
from workflows.engine import WorkflowRun, Step, engine

# Trigger: order.created



def create(context: dict) -> WorkflowRun:
    return WorkflowRun(
        workflow_name="order_confirmation",
        context=context,
        steps=[

        ],
    )


# Register with engine
engine.register("order_confirmation", create)
