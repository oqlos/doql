# Environment for CRM Contacts (docker-compose)
APP_NAME=CRM Contacts
DATABASE_URL=sqlite:///./data/app.db
DATABASE_URL=sqlite:///crm.db
DOMAIN=localhost
JWT_SECRET=change-me-in-production
SMTP_FROM=crm@example.com
SMTP_HOST=localhost
SMTP_PORT=587
