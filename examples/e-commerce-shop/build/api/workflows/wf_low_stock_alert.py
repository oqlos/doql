"""Workflow: low_stock_alert — generated by doql."""
from workflows.engine import WorkflowRun, Step, engine

# Trigger: product.updated
# Condition: product.stock < 5



def create(context: dict) -> WorkflowRun:
    return WorkflowRun(
        workflow_name="low_stock_alert",
        context=context,
        steps=[

        ],
    )


# Register with engine
engine.register("low_stock_alert", create)
