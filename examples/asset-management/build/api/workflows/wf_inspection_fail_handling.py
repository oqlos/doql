"""Workflow: inspection_fail_handling — generated by doql."""
from workflows.engine import WorkflowRun, Step, engine

# Trigger: event Inspection.completed WHERE result=fail



def create(context: dict) -> WorkflowRun:
    return WorkflowRun(
        workflow_name="inspection_fail_handling",
        context=context,
        steps=[

        ],
    )


# Register with engine
engine.register("inspection_fail_handling", create)
