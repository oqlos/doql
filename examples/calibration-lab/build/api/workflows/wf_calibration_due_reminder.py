"""Workflow: calibration_due_reminder — generated by doql."""
from workflows.engine import WorkflowRun, Step, engine

# Trigger: schedule "0 8 * * MON"



def create(context: dict) -> WorkflowRun:
    return WorkflowRun(
        workflow_name="calibration_due_reminder",
        context=context,
        steps=[

        ],
    )


# Register with engine
engine.register("calibration_due_reminder", create)
