"""Workflow: overdue_activity_reminder — generated by doql."""
from workflows.engine import WorkflowRun, Step, engine




def create(context: dict) -> WorkflowRun:
    return WorkflowRun(
        workflow_name="overdue_activity_reminder",
        context=context,
        steps=[

        ],
    )


# Register with engine
engine.register("overdue_activity_reminder", create)
