"""Workflow: ota_canary — generated by doql."""
from workflows.engine import WorkflowRun, Step, engine

# Trigger: manual



def create(context: dict) -> WorkflowRun:
    return WorkflowRun(
        workflow_name="ota_canary",
        context=context,
        steps=[

        ],
    )


# Register with engine
engine.register("ota_canary", create)
