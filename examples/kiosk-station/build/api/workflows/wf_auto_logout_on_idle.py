"""Workflow: auto_logout_on_idle — generated by doql."""
from workflows.engine import WorkflowRun, Step, engine

# Trigger: "event kiosk.idle > 180s AND session.active"



def create(context: dict) -> WorkflowRun:
    return WorkflowRun(
        workflow_name="auto_logout_on_idle",
        context=context,
        steps=[

        ],
    )


# Register with engine
engine.register("auto_logout_on_idle", create)
