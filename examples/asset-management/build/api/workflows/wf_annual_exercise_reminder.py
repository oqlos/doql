"""Workflow: annual_exercise_reminder — generated by doql."""
from workflows.engine import WorkflowRun, Step, engine

# Trigger: schedule "0 9 1 1 *"



def create(context: dict) -> WorkflowRun:
    return WorkflowRun(
        workflow_name="annual_exercise_reminder",
        context=context,
        steps=[

        ],
    )


# Register with engine
engine.register("annual_exercise_reminder", create)
