"""Workflow: cylinder_filling_audit — generated by doql."""
from workflows.engine import WorkflowRun, Step, engine

# Trigger: schedule "monthly"



def create(context: dict) -> WorkflowRun:
    return WorkflowRun(
        workflow_name="cylinder_filling_audit",
        context=context,
        steps=[

        ],
    )


# Register with engine
engine.register("cylinder_filling_audit", create)
