# Notes — API

Generated by `doql build`.

## Quick start

```bash
cd build/api
pip install -r requirements.txt
uvicorn main:app --reload
```

## Entities

- **Notebook** (4 fields)
- **Note** (7 fields)
- **Tag** (3 fields)

## Endpoints

- `GET /api/v1/notebooks` · `POST` · `GET /:id` · `PATCH /:id` · `DELETE /:id`
- `GET /api/v1/notes` · `POST` · `GET /:id` · `PATCH /:id` · `DELETE /:id`
- `GET /api/v1/tags` · `POST` · `GET /:id` · `PATCH /:id` · `DELETE /:id`
