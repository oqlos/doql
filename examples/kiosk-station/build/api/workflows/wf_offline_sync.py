"""Workflow: offline_sync — generated by doql."""
from workflows.engine import WorkflowRun, Step, engine

# Trigger: event network.online
# Condition: queue_size > 0



def create(context: dict) -> WorkflowRun:
    return WorkflowRun(
        workflow_name="offline_sync",
        context=context,
        steps=[

        ],
    )


# Register with engine
engine.register("offline_sync", create)
