"""SQLAlchemy ORM models — generated by doql."""
from sqlalchemy import Column, String, Integer, Float, Boolean, Text, Date, DateTime, JSON, ForeignKey
from sqlalchemy.orm import relationship
from database import Base


class Notebook(Base):
    __tablename__ = "notebooks"

    id = Column(String(36), primary_key=True)
    name = Column(String(255))
    color = Column(String(255), nullable=True)
    created = Column(DateTime, nullable=True)


class Note(Base):
    __tablename__ = "notes"

    id = Column(String(36), primary_key=True)
    notebook = Column(String(255), ForeignKey("notebooks.id"), nullable=True)
    title = Column(String(255))
    body = Column(Text, nullable=True)
    pinned = Column(Boolean, nullable=True, default=False)
    created = Column(DateTime, nullable=True)
    updated = Column(DateTime, nullable=True)


class Tag(Base):
    __tablename__ = "tags"

    id = Column(String(36), primary_key=True)
    name = Column(String(255), unique=True)
    color = Column(String(255), nullable=True)
