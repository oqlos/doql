"""Workflow: publish_notification — generated by doql."""
from workflows.engine import WorkflowRun, Step, engine

# Trigger: "post.updated"



def create(context: dict) -> WorkflowRun:
    return WorkflowRun(
        workflow_name="publish_notification",
        context=context,
        steps=[

        ],
    )


# Register with engine
engine.register("publish_notification", create)
